from setuptools import setup, find_packages

setup(
    name='employee_evaluation',
    version='0.0.1',
    description='Employee Evaluation App',
    author='AutoGenerated',
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=['frappe']
)
